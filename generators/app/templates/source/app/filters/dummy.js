
'use strict';

angular
	// as a deep for root module
	.module('<%=app%>')
	// filter injector name
	.filter('<%=name%>Filter', function () {
		// filter method
		return function ( string ) {
			// filter execute
			

		};
	});